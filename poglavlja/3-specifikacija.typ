= Спецификација _TestDataGen_ система 

У претходном поглављу описани су језици специфични за домен и њихове основне карактеристике. ЈСД омогућавају
мапирање концептуалног решења помоћу концепата који су блиски самом домену. На тај начин се сложени имплементациони
детаљи могу сакрити иза апстракције разумљиве корисницима и доменским експертима.

За тестирање различитих софтверских система често је неопходно припремити велику количину података. Ручно креирање
таквих података је временски захтевно. Додатни напор је потребан уколико је неопходно тестирати велики број комбинација
вредности или специфичне случајеве. Неки од примера би били граничне или недостајуће вредности. Генерисање
случајних вредности је погодно за брзо креирање велике количине података, али не решава проблем са одређеним
вредностима које треба уврстити.

Предмет мастер рада јесте проширење језика специфичног за домен под називом _TestDataGen_ @testdatagen. Његов домен је
дефинисање и аутоматско генерисање тестних података. Основна идеја је омогућавање кориснику да на
једноставан начин опише:
- структуру података које жели да генерише
- ограничења над подацима
- стратегије генерисања и
- међусобне односе.

На основу тако дефинисане спецификације, систем генерише скуп тестних података у једном или више различитих
формата.

Мана генерисања искључиво случајним избором се превазилази тако што корисник изабере стратегију на основу
које се генеришу подаци. Подржане су стратегије засноване на анализи граничних вредности, партицијама
еквиваленције и генерисању специјалних случајева. Поред тога, ЈСД омогућавају дефинисање начина на који се
вредности различитих поља међусобно комбинују, како би се избегла комбинаторна експлозија броја генерисаних
тестних података.

Корисник не описује поступак генерисања података корак по корак, него дефинише особине жељених података и
правила која генератор треба да примени. Овај приступ се назива декларативним. 

Основни концепти језика су шема, ентитет, поље и веза. У оквиру шеме (енгл. _schema_) дефинишу се глобална
подешавања. Ентитети (енгл. _entity_) представљају структуре за које се генеришу подаци, а везе (енгл.
_relationship_) дефинишу односе између ентитета. Сваки ентитет садржи поља (енгл. _field_) са одговарајућим
типовима и ограничењима. У наставку је наведен пример структуре ентитета.

```testdatagen
 entity User {
    fields {
        id: uuid
        email: email {
            unique,
            include [null, empty, invalid]
        }
        username: username {
            unique
        }
        age: number {
            range 18..99,
            boundary,
            partitions [27, 67]
        }
        status: enum ["active", "suspended", "pending"] {
            coverage 100
        }
    }
    config {
        generate: 50
        combination_strategy: each-used
        include: [
            { id: "00000000-0000-0000-0000-000000000001", email: "admin@system.local", username: "superadmin", age: 30, status: "active" },
            { id: "00000000-0000-0000-0000-000000000002", email: null, username: "guest_user", age: 17, status: "pending" }
        ]
    }
}
```

На основу овакве спецификације није потребно ручно имплементирати алгоритме за рачунање сваког појединачног тестног
случаја. Корисник опише домен података, њихове типове и ограничења, док _TestDataGen_ на основу дефинисаних правила
одреди вредности које треба генерисати. На пример за поље age могу бити дефинисане вредности из траженог
опсега, вредности непосредно изнад и испод граница и представници различитих партиција еквиваленције.

Поред аутоматски генерисаних података, _TestDataGen_ омогућава и употребу експлицитно наведених вредности.
Тиме кориснику даје могућност да укључи конкретне вредности које морају бити обухваћене у генерисаном скупу
података. То је нарочито корисно за тестирање познатих грешака и регресионо тестирање.

Важан аспект проблема који _TestDataGen_ решава представља и број могућих комбинација вредности. Уколико сваки
атрибут ентитета може да има више вредности, Декартов производ скупа могућих вредности може довести до експанзије
у броју генерисаних тестних случајева. Због тога језик подржава различите стратегије комбинације вредности и то су
потпуно комбиновање (Декартов производ могућности), комбинација парова (сваки пар се појави једном) и да се
свака генерисана вредност појави најмање једном. На тај начин корисник има могућност да одабере компромис између
жељене покривености тестних података и броја генерисаних података.

Коначан резултат обраде _TestDataGen_ спецификације може бити генерисан у једном или више формата. Подржани су
следећи формати за генерисање тестних података: _SQL_, _JSON_, _CSV_, _YAML_ и _Neo4j_ (он генерише _Cypher_
скрипту за рад са графном базом). Поред самих тестних података, систем може да генерише _HTML_ извештај о
покривености генерисаних тестних случајева (формат _REPORT_).

На овај начин _TestDataGen_ раздваја опис података и стратегије њиховог генерисања од конкретне имплементације
генератора. Корисник на вишем нивоу апстракције дефинише шта је потребно тестирати, а систем генерише одговарајуће
тестне податке. Тиме се примењују принципи језика специфичних за домен у области аутоматског генерисања
тестних података.

У наставку ће бити описани основни градивни елементи језика _TestDataGen_, његова апстрактна и конкретна синтакса,
семантика и архитектура система. Такође ће бити детаљније представљене стратегије које се користе за паметни
избор тестних података, као и имплементирани генератори излазних формата.

== Апстрактна синтакса

Апстрактна синтакса језика _TestDataGen_ дефинише концепте домена генерисања тестних података, њихове особине и
међусобне односе. Није дефинисано на који начин се концепти записују, већ само структура сваке валидно написане
спецификације у језику.

Почетни концепт апстрактне синтаксе је шема (енгл. _schema_). Једна шема представља целокупну спецификацију генерисаних
тестних података. Шема има назив, а може да садржи опис, почетну вредност за генерисање случајних бројева,
подразумевану стратегију генерисања и подразумевану стратегију комбиновања вредности. Осим тога, шема садржи скуп
елемената, који могу бити ентитети или везе. На тај начин, модел омогућава опис самих структура података и
њихових међусобних односа.

Ентитет (енгл. _entity_) представља структуру за коју се генеришу тестни подаци. Сваки ентитет има назив и скуп
поља. Поред структуре података, ентитет може да садржи и конфигурацију генерисања. Конфигурација омогућава одређивање
броја података који треба да буду генерисани, стратегије комбиновања вредности и експлицитно дефинисање тестних
случајева.

Поље (енгл. _field_) представља појединачну особину ентитета. Свако поље има назив и тип. Тип поља може да буде
један од уграђених типова података, набројиви тип или референца на други ентитет.

Уграђени типови обухватају различите типове података који се могу генерисати. То су: идентификатори, адресе
електронске поште, имена, презимена, корисничка имена, бројеви, логичке вредности, датуми и други типови података
подржани генератором. Набројиви тип (енгл. _enum_) омогућава дефинисање коначног скупа могућих вредности поља.
Референтни тип (енгл. _ref_) успоставља структурну зависност између ентитета, тако што се поље повезује са другим
ентитетом. Референца може представљати појединачну вредност или колекцију вредности, за коју је могуће дефинисати
минималан и максималан број елемената.

Поред типа, поље може да има скуп ограничења (енгл. _constraints_). Ограничења дефинишу скуп правила која утичу на
избор и генерисање вредности. Подржана су ограничења за дефинисање опсега вредности, анализу граничних случајева,
партиционисање домена, јединственост вредности, прецизност нумеричких вредности, специјалне вредности, покривеност
и укључивање вредности као што су _null_, празна или неважећа вредност. Такође је могуће дефинисати посебну
стратегију генерисања за појединачно поље.

Други основни елемент шеме је веза (енгл. _relationship_). Веза описује однос између два ентитета. Свака веза има
назив, полазни ентитет (енгл. _from_) и циљни ентитет (енгл. _to_). Поред тога, веза може да садржи сопствена својства.
Својства веза су представљена пољима (као код ентитета) са својим типовима и ограничењима.

За везу је могуће дефинисати конфигурацију. Она одређује начин генерисања односа између инстанци ентитета. Подржане
су стратегије један-према-један (енгл. _one-to-one_), један-према-више (енгл. _one-to-many_), више-према-један 
(енгл. _many-to-one_), више-према-више (енгл. _many-to-many_). Конфигурација везе може да садржи и максималан број
веза које генератор може да произведе, ограничења минималног и максималног степена повезаности, као и експлицитно
дефинисање тестних случајева.

У тренутној имплементацији, дефиниције везе се користе само приликом генерисања _Cypher_ скрипти за _Neo4j_ графну базу
података. Остали генератори обрађују структуре дефинисане ентитетима. 

Експлицитно дефинисање тестних случајева представља засебан концепт апстрактне синтаксе. Они се састоје од скупа
додела вредности пољима. Вредност може бити скаларна вредност, листа вредности или _null_. На тај начин корисник, поред
аутоматски генерисаних вредности, може да дефинише конкретне случајеве који морају бити укључени у резултат генерисања.

Однос између основних концепата апстрактне синтаксе је приказан на слици @fig:klasni.

#figure(image("../slike/classDiagram.png", width: auto),
caption: [
    Апстрактна синтакса језика _TestDataGen_.
]
)<fig:klasni>

== Конкретна синтакса

Конкретна синтакса језика _TestDataGen_ је текстуална. Спецификација се записује у облику текстуалне датотеке са екстензијом
_.tdata_ или _.tdg_. Концепти апстрактне синтаксе се представљају помоћу кључних речи, идентификатора и литерала.

Кључне речи одређују структуру спецификације и имају унапред дефинисано значење. То су _schema_, _entity_, _fields_, _config_
и _relationship_. Идентификатори се користе за именовање шема, ентитета и поља. Литерали се користе за запис конкретних
вредности, као што су цели и реални бројеви, текстуалне и логичке вредности.

Граматика конкретне синтаксе дефинисана је коришћењем алата textX @dejanovic2017a. На основу дефинисане граматике, текстуална спецификација
се парсира и претвара у објектни модел. _textX_ на основу правила граматике креира објекте који представљају
конструкте спецификације и њихове везе. Тако добијени модел се даље користи у процесу валидације и генерисања
тестних података.

=== Шема

Основни елемент конкретне синтаксе је шема. Шема се дефинише кључном речју _schema_. Након тога следи њен назив и тело
шеме унутар витичастих заграда. Унутар шеме могу се дефинисати глобална подешавања, ентитети и везе између ентитета.
Глобална подешавања су опциона и обухватају опис шеме, почетну вредност генератора случајних бројева, подразумеване
стратегије генератора и комбиновања вредности.

```testdatagen
schema ComprehensiveSystem {
    description: "Master rad"
    seed: 98765
    strategy: smart
    combination_strategy: pairwise
}
```

Атрибут _description_ омогућава додавање текстуалног описа шеме. Помоћу атрибута _seed_ задаје се почетна вредност
генератора случајних бројева. Атрибутима _strategy_ и _combination_strategy_ дефинишу се подразумеване стратегије
генерисања и комбиновања вредности. Пошто су наведена подешавања опциона, шема може садржати само назив
и елементе који се у њој дефинишу.

=== Ентитет

Ентитети се у конкретној синтакси дефинишу помоћу кључне речи _entity_. Након тога следи назив ентитета и његово тело.
Тело ентитета садржи блок _fields_. У њему се дефинишу поља. Опционо се може навести и блок _config_ са подешавањима
генератора.

Поље се записује у облику:

```testdatagen
name: type
```

где _name_ представља назив поља, а _type_ његов тип. Уколико поље има ограничења, она се наводе унутар витичастих
заграда након типа.

```testdatagen
entity Profile {
    fields {
        id: uuid
        bio: string
        birth_date: date {
            range "1950-01-01".."2005-12-31",
            boundary
            partition 7
        }
        is_verified: boolean {
            coverage 100
        }
    }
    config {
        generate: 30
    }
}
```

У наведеном примеру дефинисан је ентитет _Profile_ са четири поља. Поље _id_ има тип _uuid_. Поље _bio_ има тип _string_.
Поље _birth_date_ има тип _date_, а за њега је дефинисан опсег вредности и да се врши анализа граничних случајева. Поље
_is_verified_ има тип _boolean_, а за њега је дефинисана покривеност. Такође, дефинисана је конфигурација за ентитет са
задатим бројем генерисаних инстанци ентитета. Објашњења подржаних ограничења ће бити описана у наредним поглављима.

=== Типови поља

Тип поља може припадати једној од три категорије:
- једноставни тип
- набројиви тип и
- референтни тип.

Једноставни типови представљају унапред дефинисане типове података које генератор подржава. У њих спадају: _uuid_,
_email_, _firstName_, _lastName_, _fullName_, _number_, _boolean_, _datetime_, _date_, _string_, _productName_, _companyName_,
_address_, _city_, _country_, _phone_, _url_ и _username_.

Набројиви типови омогућавају да се скуп могућих вредности поља експлицитно наведе. Дефинишу се кључном речју _enum_.
Након тога се у угластим заградама наводе могуће вредности.

```testdatagen
status: enum ["active", "suspended", "pending"]
```

У горенаведеном примеру, генератор приликом избора вредности за поље _status_ користи само вредности наведене у дефиницији
набројивог типа.

Референтни тип се користи за успостављање везе између поља једног ентитета и другог ентитета. Дефинише се кључном
речју _ref_. Након тога следи назив ентитета на који се референца односи.

```testdatagen
user: ref User
```

Референца може представљати и колекцију вредности. У том случају се након назива ентитета наводе угласте заграде. За
колекцију је могуће дефинисати минималан и максималан број ентитета.

```testdatagen
users: ref User[] count 1..5
```

Опционим кључним речима _count_ и _boundary_ могу се додатно дефинисати правила за генерисање броја референци.

=== Ограничења над пољима

Поред типа, пољу се могу придружити ограничења која утичу на начин избора и генерисања њихових вредности. Ограничења
се наводе унутар витичастих заграда након типа поља.

_TestDataGen_ подржава више врста ограничења. Ограничењем _range_ дефинише се опсег дозвољених вредности. Ограничење
_boundary_ омогућава укључивање граничних случајева. Ограничење _partition_ омогућава одређивање на колико партиција ће бити
подељен домен. Помоћу _partitions_ се дефинишу представници партиција. Ограничење _unique_ захтева јединственост генерисаних
вредности за поље. За нумеричке вредности може се дефинисати и прецизност помоћу ограничења _precision_.

```testdatagen
age: number {
    range 18..99,
    boundary,
    partitions [27, 67]
}
```

Поред горенаведених ограничења, могуће је дефинисати специјалне вредности помоћу _special_. Кључном речју _include_,
омогућено је захтевати укључивање посебних вредности као што су _null_, празна вредност и неважећа вредност. Захтевани
проценат покривености дефинише се помоћу _coverage_. 

```testdatagen
price: number {
    range 0..500.0,
    boundary,
    precision 2,
    special ["0", "99.99", "499.99"]
}
```

За појединачно поље може се дефинисати и стратегија генерисања помоћу ограничења _strategy_. На тај начин је могуће
одступати од подразумеване стратегије дефинисања на нивоу шеме или ентитета.

=== Конфигурација генерисања

Конфигурација ентитета дефинише начин генерисања инстанци ентитета. Наводи се унутар блока _config_. Основна опција
је _generate_, којом се одређује број података који треба генерисати.

```testdatagen
 entity Course {
    fields {
        id: uuid
        title: productName
        price: number {
            range 0..500.0,
            boundary,
            precision 2,
            special ["0", "99.99", "499.99"]
        }
        capacity: number {
            range 10..200,
            partition 5
        }
    }
    config {
        generate: 20
        combination_strategy: full
    }
}
```

Поред броја генерисаних инстанци, на нивоу ентитета могуће је дефинисати стратегију комбиновања вредности помоћу
_combination_strategy_. Такође је могуће експлицитно навести тестне случајеве који морају бити укључени у резултат
генерисања. Оваква организација омогућава да се глобална правила дефинишу на нивоу шеме, док се специјална правила
могу дефинисати на нивоу појединачног ентитета или поља.

=== Везе ентитета

Односи између ентитета дефинишу се помоћу конструкције _relationship_. Полазни ентитет наводи се после кључне речи
_from_, а циљни ентитет након кључне речи _to_.

```testdatagen
relationship UserHasProfile {
    from User
    to Profile
    properties {
        linked_at: datetime {
            boundary
        }
    }
    config {
        strategy: one-to-one
        generate: 30
        include: [
            { linked_at: "2026-01-01T10:00:00" }
        ]
    }
}
```

У оквиру везе могу се дефинисати и својства помоћу блока _properties_. Својства се записују на исти начин као и поља
ентитета. Имају назив, тип и опциона ограничења.

Конфигурација везе дефинише начин генерисања односа између инстанци ентитета. Подржане су стратегије _one-to-one_,
_one-to-many_, _many-to-one_ и _many-to-many_. Додатно се могу дефинисати минимални и максимални степен повезаности
помоћу опција _minDegree_ и _maxDegree_.

У тренутној имплементацији, дефинисане везе се користе приликом генерисања _Cypher_ скрипти за _Neo4j_ графну базу података,
док остали генератори обрађују структуре дефинисане ентитетима.

=== Експлицитно дефинисање тестних случајева

Поред аутоматског генерисања вредности, _TestDataGen_ омогућава кориснику да експлицитно наведе тестне случајеве који
морају бити укључени у резултат. Они се дефинишу помоћу опције _include_. Сваки тестни случај се представља као скуп
додела вредности пољима.

```testdatagen
relationship StudentEnrollment {
    from User
    to Course
    properties {
        progress_percentage: number {
            range 0..100,
            boundary
        }
        status: enum ["enrolled", "completed", "dropped"]
    }
    config {
        strategy: many-to-many
        generate: 100
        minDegree: 1
        maxDegree: 4
        include: [
            { progress_percentage: 100.0, status: "completed" },
            { progress_percentage: 0, status: "dropped" }
        ]
    }
}
```

Додела вредности има облик _name: value_. Вредност може да буде скаларна вредност, листа скаларних вредности или _null_.
Пример листе скаларних вредности је дат у наставку:

```testdatagen
tags: [ "admin", "user" ]
```

Овим механизмом кориснику је омогућено да, поред вредности које систем самостално генерише на основу дефинисаних стратегија,
зада и конкретне случајеве који су од значаја за тестирање. То је посебно корисно за познате проблематичне случајеве,
регресионо тестирање и проверу специјалних комбинација вредности.

=== Комплетан пример

Следећи пример показује повезивање већине описаних конструкција конкретне синтаксе у једној _TestDataGen_ спецификацији:

```testdatagen
schema ComprehensiveSystem {
    description: "Master rad"
    seed: 98765
    strategy: smart
    combination_strategy: pairwise

    entity User {
        fields {
            id: uuid
            email: email {
                unique,
                include [null, empty, invalid]
            }
            username: username {
                unique
            }
            age: number {
                range 18..99,
                boundary,
                partitions [27, 67]
            }
            status: enum ["active", "suspended", "pending"] {
                coverage 100
            }
        }
        config {
            generate: 50
            combination_strategy: each-used
            include: [
                { id: "00000000-0000-0000-0000-000000000001", email: "admin@system.local", username: "superadmin", age: 30, status: "active" },
                { id: "00000000-0000-0000-0000-000000000002", email: null, username: "guest_user", age: 17, status: "pending" }
            ]
        }
    }

    entity Profile {
        fields {
            id: uuid
            bio: string
            birth_date: date {
                range "1950-01-01".."2005-12-31",
                boundary
                partition 7
            }
            is_verified: boolean {
                coverage 100
            }
        }
        config {
            generate: 30
        }
    }

    entity Course {
        fields {
            id: uuid
            title: productName
            price: number {
                range 0..500.0,
                boundary,
                precision 2,
                special ["0", "99.99", "499.99"]
            }
            capacity: number {
                range 10..200,
                partition 5
            }
        }
        config {
            generate: 20
            combination_strategy: full
        }
    }

    relationship UserHasProfile {
        from User
        to Profile
        properties {
            linked_at: datetime {
                boundary
            }
        }
        config {
            strategy: one-to-one
            generate: 30
            include: [
                { linked_at: "2026-01-01T10:00:00" }
            ]
        }
    }

    relationship StudentEnrollment {
        from User
        to Course
        properties {
            progress_percentage: number {
                range 0..100,
                boundary
            }
            status: enum ["enrolled", "completed", "dropped"]
        }
        config {
            strategy: many-to-many
            generate: 100
            minDegree: 1
            maxDegree: 4
            include: [
                { progress_percentage: 100.0, status: "completed" },
                { progress_percentage: 0, status: "dropped" }
            ]
        }
    }
}
```

Наведени пример илуструје основну организацију _TestDataGen_ спецификације. На нивоу шеме дефинисана су глобална
подешавања. У оквиру ентитета су описани структура података, типови поља и ограничења. Конфигурацијом ентитета
одређен је број инстанци које треба генерисати. Везом је описан однос између ентитета и начин генерисања.

За потребе мастер рада, комплетан пример је покренут и изгенерисани су сви излазни формати.
У наставку, сви приказани исечци се односе конкретно на изгенерисане податке овог примера. На тај начин се
јасно приказују специфичности сваког формата и олакшавају читање и разумевање резултата.

== Семантика

Семантика језика _TestDataGen_ дефинише значење валидне спецификације, односно начин на који се информације описане спецификацијом
користе за генерисање тестних података. Док конкретна синтакса одређује начин записивања спецификације, семантика одређује
шта такав запис представља и какав резултат треба да произведе.

Синтаксна исправност спецификације не гарантује да спецификација има смислено значење. Због тога се након парсирања врши
семантичка анализа модела. Тиме се проверавају услови који се не могу или их није погодно дефинисати самом граматиком.

Један од примера представља референце између ентитета. Када се поље дефинише као референца, референтни ентитет мора постојати
у оквиру шеме. На пример:

```testdatagen
entity Order {
    fields {
        user: ref User
    }
}
```

У наведеном примеру _User_ мора бити дефинисан као ентитет у шеми. Иако се постојање таквог ентитета може проверити пре
парсирања, његова исправност представља семантичко, а не синтаксно правило.

Слично томе, вредности и ограничења поља морају бити међусобно усаглашени. На пример, ограничење _precision_ има смисла
за нумеричке вредности, док се за поља типа _boolean_ не може применити исто ограничење. Семантичка анализа треба да обезбеди
да су конфигурација и ограничења применљиви на тип поља над којим су дефинисани.

Семантичка правила се примењују и на конфигурацију генератора. Број података који се генерише мора представљати позитивну
вредност. Изабране стратегије морају бити подржане за дати тип података и конфигурацију. На исти начин, минимални и максимални
степен повезаности код веза морају бити међусобно усаглашени.

=== Семантика стратегија генерисања

Један од основних делова семантике језика _TestDataGen_ представљају стратегије генерисања. Стратегије одређују начин избора
вредности за одређено поље. Подржане су стратегије _random_, _boundary_, _partition_ и _smart_ @ammann2008introduction.

Стратегија _random_ генерише вредности случајним избором из скупа вредности које одговарају типу и дефинисаним ограничењима.
Стратегија _boundary_ користи граничне вредности домена, као и вредности непосредно изнад и испод дефинисаних граница када
је то применљиво. Стратегија _partition_ користи представнике дефинисаних партиција еквиваленције. Стратегија _smart_
комбинује расположиве стратегије како би се добио скуп тестних вредности који обухвата различите захтеване случајеве.

Стратегија може бити дефинисана на нивоу шеме, конфигурације ентитета или појединачног поља. На тај начин је омогућено
дефинисање подразумеваног понашања, уз могућност да се оно измени на нижем нивоу. Уколико стратегија није експлицитно
наведена за поље, користи се одговарајућа подразумевана стратегија.

=== Семантика ограничења

Ограничења над пољима представљају правила која одређују које вредности могу бити генерисане и које тестне случајеве треба
укључити у резултат. Ограничења и њихове дефиниције су приказани у табели @tbl:ogranicenja.

#figure(
    table(
        columns: 2,
        align: (col, row) => (left,left,).at(col),
        inset: 6pt,
        [Ограничења], [Шта дефинише],
        [_rаnge_], [Дозвољени опсег вредности],
        [_boundary_], [Приликом генерисања се морају размотрити гранични случајеви],
        [_precision_], [Број децималних места на које се заокружују генерисане нумеричке вредности],
        [_partition_], [Број партиција на које се домен дели],
        [_partitions_], [Конкретне вредности представника партиција],
        [_unique_], [Генерисане вредности поља морају бити јединствене],
        [_include_], [Експлицитно укључивање вредности као што су _null_, празна или невалидна вредност],
        [_special_], [Кориснички наведене вредности које треба укључити у генерисани скуп вредности],
        [_coverage_], [Захтевани ниво покривености],
    ),
    caption: [Табела ограничења]
)<tbl:ogranicenja>

Ограничења _partition_ и _partitions_ представљају два различита начина дефинисања партиционисања. Ограничење _partition_
одређује број партиција које генератор треба да формира, а _partitions_ омогућава кориснику да наведе експлицитне представнике
партиција. Није могуће користити их заједно. Такође, користе се само код типова _number_, _date_ и _datetime_.

Ограничење _precision_ одређује број децималних места на које се заокружују генерисане вредности типа _number_.
Вредност мора да буде ненегативан цео број. Такође, ова вредност се користи и код одабира граничних вредности (_boundary_).

Ограничење _include_ омогућава експлицитно укључивање вредности које можда не би биле изабране случајним генерисањем.
Могуће је укључити само _null_, празну или невалидну вредност. Разлика код ограничења _special_ је у томе што је њом
могуће у генерисани скуп укључити експлицитно наведене вредности. Вредности се прослеђују као текстуални литерали. Води 
се рачуна о томе да се наведе барем једна вредност за укључивање.

Ограничење _coverage_ одређује захтевани ниво покривености. Задата вредност може бити од 0 до 100.

Више ограничења може бити примењено над истим пољем. У том случају, генератор приликом избора вредности мора да узме
у обзир сва дефинисана ограничења. На пример, уколико корисник жели да се приликом генерисања узму у обзир дефинисани опсег,
граничне вредности и захтевани број партиција, запис би био следећи:

```testdatagen
birth_date: date {
    range "1950-01-01".."2005-12-31",
    boundary
    partition 7
}
```

=== Семантика комбиновања вредности

Након што се за појединачна поља одреде могуће вредности, потребно је одредити на који начин ће се те вредности комбиновати
у тестне случајеве. Језик _TestDataGen_ подржава три стратегије комбиновања: _full_, _pairwise_ и _each-used_.

Стратегија _full_ формира све могуће комбинације вредности, односно Декартов производ скупова вредности појединачних поља.
Оваквим приступом обезбеђује се потпуна покривеност комбинација, али број случајева може значајно порасти са бројем поља
и њихових могућих вредности.

Стратегија  _pairwise_ настоји да обезбеди да се сваки пар вредности из различитих поља појави најмање једном, уколико је то
могуће у оквиру генерисаних случајева. На тај начин, број тестних случајева се смањује у односу на потпуно комбиновање,
уз задржавање покривености парова вредности.

Стратегија _each-used_ обезбеђује да се свака генерисана вредност употреби најмање једном. Ова стратегија додатно смањује
број комбинација када није неопходно тестирати све парове или све комбинације вредности.

=== Семантика референци

Референтна поља омогућавају повезивање генерисаних инстанци различитих ентитета. За поље типа _ref_ генератор мора да
обезбеди да изабрана вредност представља референцу на постојећу инстанцу одговарајућег ентитета.

Уколико је референца дефинисана као колекција, само тад је могуће ограничити њену величину минималним и максималним бројем елемената (ограничење _count_).
Генератор ће проверавати да ли су вредности ненегативни бројеви и да ли је минимална вредност заиста мања од максималне.
Ограничење _boundary_ је дозвољено код референци само ако је дефинисано претходно ограничење _count_. Ограничење _count_
можемо видети и на примеру где генерисана инстанца може садржати између једне и пет референци на инстанце ентитета _User_:

```testdatagen
users: ref User[] count 1..5
```

=== Семантика веза

Везе између ентитета додатно дефинишу начин повезивања њихових инстанци. Стратегије _one-to-one_, _one-to-many_,
_many-to-one_ и _many-to-many_ одређују кардиналост везе. Опције _minDegree_ и _maxDegree_ дефинишу дозвољени број веза
једне инстанце са инстанцама другог ентитета. За стратегију _one-to-one_ није дозвољено дефинисање ограничења броја веза,
јер је он дефинисан самом кардиналношћу стратегије.

Горња граница скупа свих генерисаних веза је описана ограничењем _generate_, али број генерисаних веза може бити мањи у 
зависности од изабране стратегије.

Својства веза не могу да буду типа референца на ентитет.

=== Семантика експлицитно дефинисаних тестних случајева

Експлицитно дефинисани случајеви представљају вредности које корисник захтева да буду присутне у резултату генерисања.
Они се обрађују заједно са аутоматски генерисаним вредностима. Неопходно је да не постоји више додела истом пољу при навођењу једног
тестног случаја, такође да су сва поља постојећа за тај ентитет и да су сви типови вредности одговарајући.

На пример, корисник захтева да резултујући скуп података садржи наведене комбинације вредности:

```testdatagen
entity User {
    fields {
        id: uuid
        email: email {
            unique,
            include [null, empty, invalid]
        }
        username: username {
            unique
        }
        age: number {
            range 18..99,
            boundary,
            partitions [27, 67]
        }
        status: enum ["active", "suspended", "pending"] {
            coverage 100
        }
    }
    config {
        generate: 50
        combination_strategy: each-used
        include: [
            { id: "00000000-0000-0000-0000-000000000001", email: "admin@system.local", username: "superadmin", age: 30, status: "active" },
            { id: "00000000-0000-0000-0000-000000000002", email: null, username: "guest_user", age: 17, status: "pending" }
        ]
    }
}
```

На овај начин корисник може експлицитно да дефинише познате граничне, неважеће или друге специфичне случајеве који су од
значаја за тестирање.

=== Процес обраде спецификације

Обрада _TestDataGen_ спецификације може се представити као низ корака. Текстуална спецификација се парсира на основу граматике
дефинисане у _textX_-у. Резултат парсирања је објектни модел спецификације. Добијени модел садржи објекте који одговарају елементима спецификације, као што су шема, ентитет, поље, ограничење,
конфигурациони параметар и веза између ентитета. Након тога се над моделом врши семантичка анализа и
процес провере дефинисаних правила.

Када је модел семантички исправан, на основу његове конфигурације бирају се одговарајуће стратегије генерисања. За свако
поље одређује се скуп могућих вредности, при чему се узимају у обзир његов тип, ограничења и изабрана стратегија. Добијене вредности
се затим комбинују према изабраној стратегији комбиновања.

Уколико спецификација садржи експлицитно дефинисане тестне случајеве, они се укључују у резултујући скуп. Након генерисања
и комбиновања вредности, добијени модел тестних података прослеђује се одговарајућем генератору излазног формата.

На тај начин семантика језика _TestDataGen_ повезује структуру описану апстрактном и конкретном синтаксом са стварним понашањем
система. Корисник специфицира шта жели да тестира, док семантичка правила језика одређују како ће се та спецификација
интерпретирати и како ће се на основу ње добити тестни подаци.
