= Имплементација додатних генератора излазних формата

У оквиру овог мастер рада, имплементирана су три генератора излазних формата за систем _TestDataGen_. То су: _CSV_, _YAML_
и _Neo4j_ генератор. Циљ њихове имплементације је био проширивање постојећег система могућношћу представљања генерисаних тестних
података у форматима који се разликују по начину организације и намени.

Имплементирани генератори интегрисани су у постојећи механизам система, тако да користе исти модел добијен парсирањем _TestDataGen_
спецификације и исти поступак генерисања тестних података. На тај начин логика избора и комбиновања тестних података остаје
одвојена од логике њиховог записивања у конкретан излазни формат. Постојећи механизам командне линије омогућава избор једног или
више подржаних формата приликом једног покретања система.

Сваки од имплементираних генератора има специфичан начин представљања резултата. _CSV_ генератор организује податке у табеларном
облику и креира посебну датотеку за сваки ентитет. _YAML_ генератор представља генерисане ентитете у хијерархијској текстуалној
структури. За потребе _Neo4j_ генератора, у оквиру проширења система подржано је моделовање веза између ентитета. Концепт
_relationship_ моделује везу између ентитета, док _Neo4j_ генератор те везе користи за формирање усмерених веза у графу. 
Кључна разлика _Neo4j_ генератора, у односу на све остале генераторе, јесте у томе што обрађује и податке о везама и њиховим
својствима.

У наставку су детаљно описане имплементација и специфичности ова три генератора, са посебним освртом на начин на који преузимају
резултат заједничког процеса генерисања, обрађују податке и формирају коначни излаз.

== _CSV_ генератор

_CSV_ генератор представља један од три додатна генератора имплементирана у оквиру овог мастер рада. Његова улога је да
резултат заједничког процеса генерисања тестних података прилагоди табеларном _CSV_ формату @shafranovich2008common. При томе се не имплементира
посебна логика за избор и комбиновање тестних вредности, већ се користе постојеће компоненте система. У самом генератору
реализован је поступак припреме вредности специфичан за _CSV_ формат и његово уписивање у излазне датотеке.

_CSV_ генератор је имплементиран у оквиру модула _csv_generator_. Основна класа генератора је _CSVGenerator_, која као улаз
прима парсирани _textX_ модел спецификације. Приликом иницијализације преузима се _seed_ из спецификације, уколико није
експлицитно прослеђен, и иницијализује се _FakerTypeMapper_ који се користи за генерисање вредности @hameed2025generating.

Посебна карактеристика имплементације је поновна употреба заједничког механизма за генерисање података. _CSV_ генератор
поново користи заједничке функције за прикупљање вредности, њихово комбиновање, обраду експлицитно задатих случајева и
разрешавање референци, које су имплементиране у _sql_generator_ модулу. На тај начин се исти поступак генерисања користи
независно од тога да ли ће резултат бити записан у _CSV_ или неком другом излазном формату.

=== Формирање вредности

Пре уписивања у _CSV_ датотеке, генерисане вредности потребно је прилагодити начину представљања података у _CSV_ формату.
У ту сврху имплементирана је функција _format_value_csv_, која обрађује различите типове вредности.

Функција посебно обрађује _None_, логичке вредности, бројеве, датуме, датум и време, текстуалне вредности и листе. _None_
се представља празним пољем. Логичке вредности се записују малим словима (_true_ и _false_). Вредности типа _date_ и _datetime_
се конвертују у _ISO_ формат. Листе се представљају као вредности раздвојене тачком и зарезом унутар једне _CSV_ ћелије. На тај
начин се и листе идентификатора (настале код референци на више инстанци другог ентитета) представљају као једна вредност у _CSV_
ћелији, при чему су појединачни идентификатори ентитета раздвојени тачком и зарезом.

Кључни део имплементације приказан је у листингу @lst:vrednosti_csv.

#figure(
```python 
def format_value_csv(value: Any) -> Any:
    if value is None:
        return ""
    if isinstance(value, bool):
        return "true" if value else "false"
    if isinstance(value, (int, float)):
        return value
    if isinstance(value, datetime):
        return value.isoformat()
    if isinstance(value, date):
        return value.isoformat()
    if isinstance(value, list):
        return ";".join("" if v is None else str(format_value_csv(v)) for v in value)
    if isinstance(value, str):
        return value
    return str(value)
```,
    caption: [Функција за формирање вредности за _CSV_ формат]
) <lst:vrednosti_csv>

Ова функција представља један од специфичних делова _CSV_ генератора, јер се њоме вредности добијене из заједничког механизма
генерисања преводе у представу погодну за упис у _CSV_ датотеку.

=== Генерисање _CSV_ датотека

Резултат генерисања организован је тако да се за сваки ентитет креира посебна _CSV_ датотека. Пре самог уписа, метода _generate_
позива заједнички поступак _build_, након чега се добијени подаци прослеђују функцији за писање метаподатака и функцији за писање
_CSV_ датотека.

Упис података за појединачни ентитет реализован је методом _write_entity_csv_. Назив ентитета се користи за формирање назива
датотеке. На пример за ентитет _User_ креира се датотека _User.csv_. За упис се користи _csv.DictWriter_, при чему се називи
колона преузимају из кључева првог генерисаног реда. Након уписа заглавља, сваки генерисани ред се уписује у датотеку.

Имплементирани део кода се налази у листингу @lst:upis_csv.

#figure(
```python 
def _write_entity_csv(
        self,
        output_dir: str,
        entity_name: str,
        rows: List[Dict[str, Any]],
    ) -> None:
        path = os.path.join(output_dir, f"{entity_name}.csv")

        with open(path, "w", newline="", encoding="utf-8") as f:
            if not rows:
                return

            writer = csv.DictWriter(
                f,
                fieldnames=list(rows[0].keys()),
            )

            writer.writeheader()

            for row in rows:
                writer.writerow(row)
```,
    caption: [Функција за креирање _CSV_ датотеке]
) <lst:upis_csv>

Садржај једне _CSV_ датотеке је приказан у следећем исечку. Заглавље одговара пољима ентитета _User_, а сваки ред
једној генерисаној инстанци.

```csv
id,email,username,age,status
f86c882c-f9af-4a56-8215-c2b76947c104,cody82@example.com,adamsdaniel,98.0,active
a1ada731-4f99-436e-8772-2de6c8c22ed4,murraycandice@example.com,johnsontony,5,active
```

=== Организација излазног директоријума

Поред самог писања _CSV_ датотека, генератор формира и директоријум за резултат генерисања. Назив директоријума одговара
називу шеме. Уколико директоријум већ постоји, његово преписивање зависи од вредности опције _overwrite_. Без ове опције систем
пријављује грешку, док се са омогућеном опцијом преписивања постојећи директоријум уклања и креира се нови.

Пример за шему _ComprehensiveSystem_, структура резултата ће бити организована на следећи начин:

```text
ComprehensiveSystem/
├─── User.csv
├─── Profile.csv
├─── Course.csv
└─── generation_details.txt
```

Оваква организација омогућава да се подаци различитих ентитета обрађују независно,
што је посебно погодно за даљи увоз или анализу података.

=== Датотека са подацима о генерисању

Поред _CSV_ датотека, генератор креира и текстуалну датотеку _generation_details.txt_. Ова датотека не садржи тестне податке,
већ метаподатке о извршеном генерисању. У њу се уписују назив шеме, коришћена почетна вредност за генератор случајних бројева,
време генерисања, излазни формат, број ентитета и укупан број генерисаних записа. За сваки ентитет се додатно наводи број
генерисаних записа. 

Део имплементације који се односи на генерисање података је приказан у листингу @lst:detalji_csv.

#figure(
```python 
def _write_generation_details(
        self,
        output_dir: str,
        metadata: Dict[str, Any],
        entities: Dict[str, List[Dict[str, Any]]],
    ) -> None:
        path = os.path.join(output_dir, "generation_details.txt")

        with open(path, "w", encoding="utf-8") as f:
            f.write("TestDataGen CSV Generation\n")
            f.write("==========================\n\n")

            f.write(f"Schema: {metadata['schema']}\n")
            f.write(f"Seed: {metadata['seed']}\n")
            f.write(f"Generated at: {metadata['generated_at']}\n\n")

            total = sum(len(rows) for rows in entities.values())

            f.write(f"Format: CSV\n")
            f.write(f"Output directory: {metadata['schema']}\n")
            f.write(f"Entities: {len(entities)}\n")
            f.write(f"Total records: {total}\n\n")

            f.write("Entities\n")
            f.write("-----------------\n")
            for entity_name, rows in entities.items():
                f.write(f"{entity_name}: {len(rows)} records\n")
```,
    caption: [Функција која генерише метаподатке за _CSV_ формат]
) <lst:detalji_csv>

Пример резултата горенаведене функције (листинг @lst:detalji_csv) је дат у наставку:

```text
TestDataGen CSV Generation
==========================

Schema: ComprehensiveSystem
Seed: 98765
Generated at: 2026-09-02 22:04:29

Format: CSV
Output directory: ComprehensiveSystem
Entities: 3
Total records: 100

Entities
-----------------
Course: 20 records
Profile: 30 records
User: 50 records
```

Датотека је подељена по секцијама. Прва секција садржи информације о шеми за коју су подаци генерисани, коришћеном _seed_-у и
времену генерисања. Друга секција садржи информације о излазном формату, директоријуму у који су резултати смештени, броју
ентитета и укупном броју генерисаних записа. Трећа секција приказује број генерисаних записа за сваки ентитет. На тај начин
генерисање не садржи само тестне случајеве већ и основне информације које омогућавају лакше праћење начина на који је
одређени скуп података настао.

=== Интеграција са остатком система

Коначно, _CSV_ генератор је интегрисан у постојећи интерфејс командне линије. Функција _generate_csv_ формира директоријум
за изабрану шему, проверава да ли он већ постоји и, у зависности од операције _overwrite_, креира нови или пријављује
грешку о постојећем директоријуму. Након тога креира се инстанца _CSVGenerator_ и покреће њена метода _generate_.

На овај начин _CSV_ генератор постаје равноправан део система и може се позвати преко постојећег механизма за избор формата
без измене основног процеса парсирања и генерисања тестних података.

== _YAML_ генератор

_YAML_ генератор представља други од три додатна генератора имплементирана у оквиру овог мастер рада. Његова улога је да
резултат заједничког процеса генерисања тестних података представи у _YAML_ формату @evans2017yaml. Као и код осталих генератора, сам
поступак избора и комбиновања вредности није део _YAML_ генератора, већ се користи постојећи механизам система. Специфичност
овог генератора огледа се у начину припреме и серијализације добијених вредности у хијерархијску _YAML_ структуру.

_YAML_ генератор је имплементиран у оквиру модула _yaml_generator_, а основна класа је _YAMLGenerator_. Класа као улаз
прима парсирани _textX_ модел спецификације, док се приликом иницијализације могу задавати почетна вредност генератора
случајних бројева (енгл. _seed_), време генерисања и ниво увлачења (енгл. _indent_). Подразумевано се користи _seed_ из спецификације
уколико друга вредност није експлицитно прослеђена.

Као и _CSV_ генератор, _YAML_ генератор поново користи функције заједничког процеса генерисања из модула _sql_generator_.
На тај начин се користе постојећи механизми за прикупљање вредности, њихово комбиновање, обраду експлицитно задатих случајева,
одређивање редоследа ентитета и разрешавање референци. Сам генератор је зато усмерен на представљање већ генерисаних
података у _YAML_ формату.

=== Формирање вредности

При серијализацији података потребно је прилагодити вредности типовима које _YAML_ библиотека може да обради. У ту сврху
имплементирана је функција _format_value_yaml_. Она прима вредност добијену из заједничког механизма генерисања и враћа
_YAML_-серијализабилну представу.

Функција посебно обрађује _None_, логичке вредности, целе и реалне бројеве, датуме, датум и време, текстуалне вредности
и листе. За разлику од _CSV_ генератора, где се _None_ представља празним пољем, у _YAML_ генератору се задржава _None_,
што се приликом серијализације представља као _YAML null_. Логичке вредности остају непромењене. Вредности типа _date_ и
_datetime_ представљају се у _ISO_ текстуалном формату. Листе се рекурзивно обрађују тако да њихови елементи задрже 
одговарајућу структуру.

Кључни део имплементације је наведен у листингу @lst:vrednosti_yaml.

#figure(
```python
def format_value_yaml(value: Any) -> Any:
    if value is None:
        return None
    if isinstance(value, bool):
        return value
    if isinstance(value, (int, float)):
        return value
    if isinstance(value, datetime):
        return value.isoformat()
    if isinstance(value, date):
        return value.isoformat()
    if isinstance(value, list):
        return [format_value_yaml(v) for v in value]
    if isinstance(value, str):
        return value
    return str(value)
```,
    caption: [Функција за формирање вредности за _YAML_ формат]
) <lst:vrednosti_yaml>

Оваква имплементација омогућава да се типови вредности не претварају непотребно у текст. На пример, логичке вредности
остају логичке вредности, док листа остаје _YAML_ секвенца. Тиме се чува структурна природа података приликом њихове
серијализације.

=== Структура _YAML_ излаза

Резултујући _YAML_ документ организован је у две основне целине: _metadata_ и _entities_. Секција _metadata_ садржи информације
о шеми, коришћеном _seed_-у и времену генерисања. У секцији _entities_ налазе се генерисани подаци организовани према називима
ентитета. Оваква структура омогућава да се у једном документу обједине метаподаци о генерисању и сами тестни подаци.

Пример структуре _YAML_ излаза је:

```yaml
metadata:
  schema: ComprehensiveSystem
  seed: 98765
  generated_at: '2026-09-02 22:04:29'
entities:
 User:
  - username: adamsdaniel
    age: 98.0
    status: active
    id: f86c882c-f9af-4a56-8215-c2b76947c104
    email: cody82@example.com
  - username: johnsontony
    age: 5
    status: active
    id: a1ada731-4f99-436e-8772-2de6c8c22ed4
    email: murraycandice@example.com
  - username: graynathan
    age: 19
    status: active
    id: 851e9b42-07a7-4c57-9254-c0373286f0cd
    email: ahurst@example.net
```

У секцији _entities_ сваки ентитет представљен је као засебна ставка, чија је вредност листа генерисаних инстанци. Свака
инстанца садржи поља ентитета и њихове генерисане вредности. На овај начин структура _YAML_ документа директно одражава
структуру података добијену из спецификације.

=== Обрада референци

Посебна пажња у имплементацији посвећена је референцама између ентитета. _YAML_ генератор користи исти механизам за разрешавање
референци као и остали генератори који раде над подацима типа ентитет. Приликом генерисања вредности референтна поља се повезују
са идентификаторима већ генерисаних инстанци одговарајућег ентитета.

Код референци на више инстанци, односно референци облика _ref Entity[]_, вредности се у _YAML_ излазу представљају као
листа идентификатора, а не као угњеждени објекти. На тај начин се задржавају информације о повезаности ентитета, без
непотребног понављања целокупних објеката. Овакав приступ је експлицитно предвиђен у имплементацији _YAML_ генератора.

=== Серијализација _YAML_ документа

Након што се подаци генеришу и припреме за _YAML_ представу, они се серијализују коришћењем библиотеке _PyYAML_ @carcano2026yaml. Увоз
библиотеке _yaml_ налази се директно у модулу генератора, док је функција _format_value_yaml_ задужена за припрему
појединачних вредности позвана пре њихове серијализације.

На овај начин _YAML_ генератор раздваја два корака: прво се вредности добијене из заједничког механизма прилагођава
за _YAML_ серијализацију, а затим се целокупна структура записује као _YAML_ документ. За разлику од _CSV_ генератора,
где је потребно ручно формирати табеларни запис и заглавље, _YAML_ генератор задржава хијерархијску структуру података.

=== Интеграција са остатком система

_YAML_ генератор је интегрисан у постојећи механизам командне линије и може се покренути избором формата _yaml_. На тај начин
се _YAML_ може генерисати самостално или истовремено са осталим подржаним излазним форматима. Документација система
наводи _yaml_ као један од формата који се могу задати приликом покретања команде _generate_.

Оваква интеграција омогућава да се иста _TestDataGen_ спецификација обради једним заједничким поступком, а затим представи
у више различитих формата. _YAML_ генератор при томе не уводи посебан механизам за генерисање тестних вредности, већ користи
постојећи модел и заједнички процес генерисања, док је његова основна одговорност формирање и серијализација _YAML_ излаза.
Оваква организација је у складу са архитектуром система у којој генератори деле заједнички модел и _pipeline_, а разликују
се у начину представљања резултата.

== _Neo4j_ генератор 

_Neo4j_ генератор представља трећи додатни генератор излазног формата у оквиру овог мастер рада. За разлику од _CSV_
и _YAML_ генератора чији је основни задатак представљање генерисаних података у другом текстуалном формату, _Neo4j_
генератор уводи могућност генерисања података који, поред самих ентитета, садрже и експлицитно дефинисане везе
између њих.

Генератор производи _Cypher_ скрипту намењену за извршавање у графној бази података _Neo4j_ @panzarino2014learning. Инстанце ентитета
представљају се као чворови графа, док се везе између ентитета представљају као усмерене везе између одговарајућих
чворова. На тај начин је омогућено генерисање тестних графова који поред вредности атрибута, садрже и структуру
повезаности између података.

Имплементација _Neo4j_ генератора захтевала је проширење више делова система. Поред самог генератора излазног формата,
било је потребно проширити граматику језика _TestDataGen_ конструкцијом везе (енгл. _relationship_), увести механизме
генерисања различитих типова веза, проширити валидацију спецификације и додати подршку за постојеће елементе _Visual
Studio Code_ екстензије.

Као и код осталих генератора, _Neo4j_ генератор користи парсирани _textX_ модел и заједнички поступак генерисања
вредности. На тај начин се генерисање вредности поља ентитета и генерисање вредности својстава веза између ентитета
ослањају на постојеће механизме система, док је специфичност генератора у формирању графичке структуре и њеном записивању
у _Cypher_ формату.

=== Проширење граматике језика

Увођење _Neo4j_ генератора захтевало је проширење граматике језика _TestDataGen_ тако да спецификација, поред ентитета,
може да садржи и експлицитно дефинисане везе између њих. У постојећој граматици _SchemaElement_ је проширен тако
да може представљати или _Entity_ или _Relationship_.

Основна структура конструкције _relationship_ дефинисана је на следећи начин:

```textX
Relationship:
    'relationship' name=ID '{'
        'from' from=[Entity]
        'to' to=[Entity]
        ('properties' '{' properties+=Field '}')?
        ('config' '{' config=RelationshipConfig '}')?
    '}'
;
```

Овом конструкцијом свака веза има назив, полазни ентитет и циљни ентитет. Додатно веза може да садржи блок својстава
везе (енгл. _properties_) за дефинисање својстава и конфигурациони блок (енгл. _config_) за подешавања начина генерисања везе.

Пример везе између ентитета _User_ и _Profile_ дат је у наставку.

```testdatagen
relationship UserHasProfile {
    from User
    to Profile
    properties {
        linked_at: datetime {
            boundary
        }
    }
    config {
        strategy: one-to-one
        generate: 30
        include: [
            { linked_at: "2026-01-01T10:00:00" }
        ]
    }
}
```

У горенаведеном примеру _User_ представља полазни, а _Profile_ циљни ентитет. Веза _UserHasProfile_ има једно својство,
_linked_at_. Вредност својства се генерише на исти начин као вредност поља ентитета. Конфигурацијом је затим одређено
да се користи стратегија _one-to-one_, као и то да је потребно генерисати највише 30 веза и укључити једну експлицитно
дефинисану вредност за својство.

Граматика омогућава да својства веза користе постојеће типове и ограничења који се користе и за поља ентитета.
Истовремено, референтни типови нису дозвољени као својства веза, јер се повезивање чворова одређује само конструкцијом
_from_ и _to_ . Ово правило се проверава у фази валидације модела.

Конфигурацијом се одређује начин генерисања везе. То подразумева контролу броја и структуре генерисаних веза.
Граматика подржава следеће опције у оквиру конфигурације:

```textX
RelationshipConfigOption:
    GenerateOption |
    RelationshipStrategyOption |
    MinDegreeOption |
    MaxDegreeOption |
    IncludeOption
;

RelationshipStrategyOption:
    'strategy:' strategy=RelationshipStrategy
;

RelationshipStrategy:
    'one-to-one'
    | 'one-to-many'
    | 'many-to-one'
    | 'many-to-many'
;

MinDegreeOption:
    'minDegree:' value=INT
;

MaxDegreeOption:
    'maxDegree:' value=INT
;
```

Опција _GenerateOption_ се односи на горњу границу броја веза који треба генерисати. Опција _RelationshipStrategyOption_
се односи на врсту стратегије при повезивању. Могуће опције су _one-to-one_, _one-to-many_, _many-to-one_ и _many-to-many_.
Опције за минималан и максималан степен чвора (енгл. _MinDegreeOption_ и _MaxDegreeOption_) дефинишу доњу и горњу
границу броја веза коју треба генерисати за неки чвор. Опција _IncludeOption_ омогућава да се укључе експлицитне вредности
својстава уколико су претходно дефинисана за неку везу, идентично као код ентитета.

=== Семантичка анализа

Поред синтаксне провере коју обезбеђује _textX_, у модулу _grammar_louder_ додатно се проверава и семантика. За потребе
валидације семантике су додате две функције _validate_relationship_ и _validate_relationship_config_.

Семантичка анализа подразумева:
- проверу да ли веза има назив
- проверу да ли су дефинисани полазни и циљни ентитети
- проверу својстава
    - да ли постоје дупликати својстава
    - да својство не може да буде референтни објекат или листа референтних објеката
- проверу конфигурације
    - максималан број генерисаних веза мора бити позитиван број
    - минимални и максимални степен чвора не смеју бити негативне вредности
    - ако су минимални и максимални степен наведени, минималан степен не сме бити већи од максималног степена чвора
    - за стратегију _one-to-one_ није дозвољено дефинисање минималног и максималног степена чвора (код _one-to-one_
        стратегије је то имплицитно јер подразумева да се једна инстанца упарује са највише једном инстанцом другог
        ентитета).

Овакав принцип валидације је значајан јер спречава да се спецификације са неисправним конфигурацијама проследе генератору.
На тај начин се грешке у спецификацији откривају пре самог генерисања излазне _Cypher_ скрипте.

=== Генерисање чвора

У _Neo4j_ модулу сваки ентитет представља тип чвора, док свака његова генерисана инстанца представља конкретан чвор.
Приликом формирања чвора користе се генерисане вредности поља ентитета.

Специфичност овог корака је у томе што се референтна поља ентитета не записују као својство чвора. Улога референтних поља је да
представљају повезаност између ентитета, док се код _Neo4j_ генератора та повезаност моделује експлицитним _relationship_
конструкцијама. На тај начин се референтна поља не додају као својства чвора, него се њихова семантика представља одговарајућим
графним везама. 

Кључни део имплементације је приказан у листингу @lst:cvorovi.

#figure(
```python
def _build_nodes(self, entity, rows):
    for row in rows:
        properties = {}

        for field in entity.fields:
            if _is_simple_ref(field) or _is_array_ref(field):
                continue

            properties[field.name] = row.get(field.name)

        yield {
            "label": entity.name,
            "properties": properties,
        }
```,
    caption: [Функција за креирање чворова]
) <lst:cvorovi>

Назив ентитета се користи као ознака (енгл. _label_) чвора, док се вредности његових поља смештају у својства чвора.
Референтна поља се прескачу, јер се везе које представљају повезаност између ентитета касније обрађују као експлицитне
графне везе. 

Пре генерисања веза неопходно је обезбедити идентификаторе генерисаних инстанци. Генератор зато памти идентификаторе
сваког ентитета. При избору идентификатора предност имају поље _id_, затим поље типа _uuid_, а уколико таква поља
не постоје користи се прво дефинисано поље ентитета.

Ови идентификатори се касније користе за одређивање конкретних полазних и циљних чворова приликом креирања веза.

=== Генерисање својстава везе

Поред самог повезивања чворова, _TestDataGen_ омогућава да и везе имају сопствена својства. Она се дефинишу у блоку
_properties_ унутар конструкције _relationship_ @robinson2015graph. Својства веза обрађују се кроз основни механизам генерисања
вредности који се користи за поља ентитета. На тај начин и за својства веза могу да се користе подржани типови,
стратегије, ограничења и експлицитно дефинисани тестни случајеви.

Оваква организација омогућава да се, на пример, уз везу између корисника и профила генеришу и додатне информације
као што је датум повезивања. Пример је приказан у наставку:

```testdatagen
    relationship UserHasProfile {
        from User
        to Profile
        properties {
            linked_at: datetime {
                boundary
            }
        }
        config {
            strategy: one-to-one
            generate: 30
            include: [
                { linked_at: "2026-01-01T10:00:00" }
            ]
        }
    }
```

=== Стратегије генерисања веза

Једна од најзначајнијих специфичности _Neo4j_ генератора је подршка различитих кардиналности веза. У имплементацији
су издвојене четири стратегије: _one-to-one_, _one-to-many_, _many-to-one_ и _many-to-many_.

Избор стратегије се врши у конфигурационом блоку везе. Метода _build_relationships_ прво преузима конфигурацију везе, а
затим се, на основу изабране стратегије, позива одговарајућа функција генерисања веза. У листингу @lst:veze је приказана
метода из модула _neo4j_generator_.

#figure(
```python
def _build_relationships(self, relationship):
    options = _relationship_options(
        getattr(relationship, "config", None)
    )
    strategy = options["strategy"]
    from_obj = getattr(relationship, "from_", getattr(relationship, "from", None))
    from_name = from_obj.name if from_obj else None

    from_rows = self._generated_rows.get(from_name, [])
    to_rows = self._generated_rows.get(relationship.to.name, [])

    if not from_rows or not to_rows:
        return iter(())
    prop_rows = self._generate_relationship_rows(relationship)

    kwargs = dict(
        relationship=relationship,
        from_rows=from_rows,
        to_rows=to_rows,
        prop_rows=prop_rows,
        generate=options["generate"],
        min_degree=options["min_degree"],
        max_degree=options["max_degree"],
        seed=self.seed,
    )

    if strategy == "one-to-one":
        return _one_to_one(**kwargs)
    if strategy == "one-to-many":
        return _one_to_many(**kwargs)
    if strategy == "many-to-one":
        return _many_to_one(**kwargs)
    if strategy == "many-to-many":
        return _many_to_many(**kwargs)
    return _one_to_one(**kwargs)
```,
    caption: [Функција за генерисање веза]
) <lst:veze>

Као што се види, прво се преузимају параметри из модела, затим се генеришу својства и на крају позива одговарајућа
стратегија генерисања веза. Уколико стратегија није експлицитно наведена, у тренутној имплементацији се користи
подразумевана _one-to-one_ стратегија. Све стратегије су издвојене у директоријум _strategies_, у модулу _relationship_.

==== Стратегија _one-to-one_

Код стратегије _one-to-one_ свака инстанца полазног ентитета се упарује са највише једном инстанцом циљног ентитета.
У имплементацији се број веза одређује као минимум броја полазних и циљних инстанци, додатно ограничено вредношћу
_generate_ уколико је она задата. 

Имплементација је приказана у листингу @lst:one_to_one.

#figure(
```python
def _one_to_one(relationship, from_rows, to_rows, prop_rows=None,
    generate=None, min_degree=None, max_degree=None, seed=None,
):
    limit = min(len(from_rows), len(to_rows))
    if generate is not None:
        limit = min(limit, generate)
    
    from_obj = getattr(relationship, "from_", getattr(relationship, "from", None))
    from_name = from_obj.name if from_obj else None
    prop_rows = prop_rows or []

    for i in range(limit):
        properties = prop_rows[i] if i < len(prop_rows) else {}
        yield {
            "from_label": from_name,
            "from_id": from_rows[i]["id"],
            "to_label": relationship.to.name,
            "to_id": to_rows[i]["id"],
            "type": relationship.name.upper(),
            "properties": properties,
        }
```,
    caption: [Стратегија _one-to-one_]
) <lst:one_to_one>

На овај начин се прва инстанца полазног ентитета повезује са првом инстанцом циљног, а друга са другом и тако редом.
Због тога је број генерисаних веза ограничен бројем доступних инстанци оба (полазног и циљног) ентитета.

==== Стратегија _one-to-many_

Код стратегије _one-to-many_ једна инстанца полазног ентитета може бити повезана са више инстанци циљног ентитета.
За сваки полазни чвор се одређује насумично степен повезаности у оквиру задатих граница (користе се _minDegree_ и _maxDegree_
ако су наведене или ако нису онда се користе подразумеване вредности). Након тога се из циљних инстанци бира одговарајући број
различитих инстанци. То је дато у исечку имплементације у листингу @lst:one_to_many.

#figure(
```python
for source in from_rows:
    degree = rng.randint(min_degree, max_degree)
    targets = rng.sample(to_rows, degree)

    for target in targets:
        if generate is not None and generated >= generate:
            return
```,
    caption: [Стратегија _one-to-many_]
) <lst:one_to_many>

Коришћење _random.sample_ обезбеђује се да се у оквиру једног полазног чвора исти циљни чвор не изабере више пута.
Укупан број генерисаних веза додатно се ограничава параметром _generate_, ако је наведен.

==== Стратегија _many-to-one_

Стратегија _many-to-one_ представља обрнути случај у односу на _one-to-many_. Свака полазна инстанца добија везу
ка изабраној инстанци циљног ентитета. Циљни чвор се бира случајно па због тога више различитих полазних чворова
може бити повезано са истим циљним чвором.

Листинг @lst:many_to_one приказује најбитнији исечак ове стратегије.

#figure(
```python
for i, source in enumerate(from_rows[:limit]):
    properties = prop_rows[i] if i < len(prop_rows) else {}
    yield {
        "from_label": from_name,
        "from_id": source["id"],
        "to_label": relationship.to.name,
        "to_id": rng.choice(to_rows)["id"],
        "type": relationship.name.upper(),
        "properties": properties,
    }
```,
    caption: [Стратегија _many-to-one_]
) <lst:many_to_one>

Број генерисаних веза ограничен је бројем полазних инстанци и, уколико је наведена, вредношћу ограничења _generate_.

==== Стратегија _many-to-many_

Стратегија _many-to-many_ омогућава да свака полазна инстанца буде упарена са више циљних инстанци. За сваки полазни
чвор насумично се бира степен повезаности. Степен повезаности мора бити у границама између ограничења _minDegree_
и _maxDegree_. Након тога се из циљних инстанци бира одговарајући број различитих чворова.

Исечак имплементације се налази у листингу @lst:many_to_many.

#figure(
```python
min_degree = min_degree or 1
max_degree = max_degree or len(to_rows)
max_degree = min(max_degree, len(to_rows))

for source in from_rows:
    degree = rng.randint(min_degree, max_degree)
    targets = rng.sample(to_rows, degree)

    for target in targets:
        if generate is not None and generated >= generate:
            return
```,
    caption: [Стратегија _many-to-many_]
) <lst:many_to_many>

На овај начин је могуће формирати густу мрежу повезаности две групе чворова. Истовремено, параметар _generate_
представља горњу границу укупног броја генерисаних веза.

=== Формирање _Cypher_ скрипте

Након што су генерисани чворови и везе, добијена структура се претвара у _Cypher_ скрипту. У ту сврху _Neo4j_ генератор
користи функцију _format_value_neo4j_, која вредности добијене из _Python_ модела преводи у одговарајуће _Cypher_
литерале.

Функција обрађује различите типове вредности и конвертује их у текстуалну вредност. _None_ се представља као _Cypher_
вредност _null_, логичке вредности као _true_ и _false_, бројеви остају бројеви, вредности типа _date_ и _datetime_ се записују као текстуалне
вредности у _ISO_ формату, текст се ставља под наводнике, а листе се рекурзивно претварају у _Cypher_ листе.

Имплементација методе је дата у листингу @lst:vrednosti_neo4j.

#figure(
```python
def format_value_neo4j(value: Any) -> str:
    if value is None:
        return "null"
    if isinstance(value, bool):
        return "true" if value else "false"
    if isinstance(value, (int, float)):
        return str(value)
    if isinstance(value, datetime):
        return json.dumps(value.isoformat(), ensure_ascii=False)
    if isinstance(value, date):
        return json.dumps(value.isoformat(), ensure_ascii=False)
    if isinstance(value, list):
        return "[" + ", ".join(
            format_value_neo4j(v)
            for v in value
        ) + "]"
    return json.dumps(str(value), ensure_ascii=False)
```,
    caption: [Функција за формирање вредности за _Neo4j_ формат]
) <lst:vrednosti_neo4j>

На овај начин је омогућено да се генерисане вредности директно користе при формирању _Cypher_ израза.

_Cypher_ скрипта се формира у две основне целине. Прва целина садржи дефиниције чворова. Друга целина садржи дефиниције
веза.

За сваки чвор генерише се _CREATE_ израз. Назив ентитета се користи као ознака чвора. Генерисане вредности својстава
служе за формирање својства чвора. Имплементација је дата у листингу @lst:iskaz_create.

#figure(
```python
for node in data["nodes"]:
    label = node["label"]
    properties = node["properties"]

    props = [
        f"{key}: {format_value_neo4j(value)}"
        for key, value in properties.items()
    ]

    lines.append(
        f"CREATE (:{label} {{{', '.join(props)}}});"
    )
```,
    caption: [Формирање _CREATE_ исказа за сваки чвор]
) <lst:iskaz_create>

На примеру за ентитет _User_ може се добити следећи _Cypher_ запис:

```cypher
CREATE (:User {id: "f86c882c-f9af-4a56-8215-c2b76947c104", email: "cody82@example.com", username: "adamsdaniel", age: 98.0, status: "active"});
```

Након чворова, генератор формира записе веза. За сваку везу најпре се помоћу _MATCH_ израза проналазе полазни и циљни
чвор на основу идентификатора. Затим се помоћу _CREATE_ израза формира усмерена веза, али се назив везе записује великим
словима. Део имплементације задужен за креирање веза је приказан у листингу @lst:iskaz_match.

#figure(
```python
for relationship in data["relationships"]:
    props = [
        f"{key}: {format_value_neo4j(value)}"
        for key, value in relationship['properties'].items()
    ]
    props_str = f" {{{', '.join(props)}}}" if props else ""

    lines.extend([
        (
            f"MATCH (from:{relationship['from_label']} "
            f"{{id: {format_value_neo4j(relationship['from_id'])}}}), "
            f"(to:{relationship['to_label']} "
            f"{{id: {format_value_neo4j(relationship['to_id'])}}})"
        ),
        f"CREATE (from)-[:{relationship['type']}{props_str}]->(to);",
        "",
    ])
```,
    caption: [Формирање _MATCH_ и _CREATE_ исказа за сваку везу]
) <lst:iskaz_match>

Резултат овог дела имплементације може дати следећи излаз:

```cypher
MATCH (from:User {id: "f86c882c-f9af-4a56-8215-c2b76947c104"}), (to:Profile {id: "a6b48acc-f413-4c34-b97c-ca3c923140ce"})
CREATE (from)-[:USERHASPROFILE {linked_at: "2028-12-12T13:33:47"}]->(to);
```

На тај начин у коначној скрипти постоје одвојени делови за чворове и везе које их повезују.

=== Проширење екстензије

Систем _TestDataGen_ садржи екстензију за _Visual Studio Code_. Подржани су: бојење текста (енгл. _syntax highlighting_),
приказ кратког описа (енгл. _hover_), предлог допуне текста, одлазак на дефиницију (енгл. _go to definition_) и проналажење референци (енгл.
_find references_).

Увођење конструкције _relationship_ захтевало је и проширење екстензије за подршку језика _TestDataGen_ @bork2023language. Поред тога
што је конструкција морала бити препозната приликом обраде спецификације, било је потребно омогућити кориснику да лакше
користи _Visual Studio Code_. Ово проширење није обухватило на одлазак на дефиницију, јер је та функционалност потребна
само ентитетима који могу бити референцирани.

==== Бојење текста (енгл. _syntax highlighting_)

Проширење језика захтевало је допуну правила за синтаксно бојење текста. Нове кључне речи конструкције везе, као и
вредности стратегија везе, издвојене су као елементи језика који треба да имају одговарајућу представу. На овај начин
корисник приликом писања спецификације визуелно може да разликује кључне речи _relationship_, _from_, _to_, _properties_
и називе стратегија од осталог текста.

На слици @fig:bojenje приказан је пример светле теме за имплементирану екстензију _Visual Studio Code_-а.

#figure(image("../slike/highlighting.png", width: auto),
caption: [
    Синтаксно бојење у _Visual Studio Code_.
]
)<fig:bojenje>

==== Приказ кратког описа (енгл. _hover_)

У _Language Server_-у су проширени речници који описују елементе језика. Кључне речи _relationship_, _from_, _to_ и
_properties_ су додате у речник кључних речи. Такође је уведен речник стратегија веза, што је приказано у листингу
@lst:recnici.

#figure(
```python
KEYWORDS_DICTIONARY = {
    ...
    "relationship": "Defines a directed relationship between two entities.",
    "properties": "Block containing properties stored on a relationship.",
    "from": "Source entity of the relationship.",
    "to": "Target entity of the relationship.",
    ...
}

RELATIONSHIP_STRATEGY_DICTIONARY = {
    "one-to-one": "Each source entity is connected to exactly one target entity.",
    "one-to-many": "Each source entity may be connected to multiple target entities.",
    "many-to-one": "Multiple source entities may be connected to the same target entity.",
    "many-to-many": "Source and target entities may have multiple connections.",
}
```,
    caption: [Речници потребни за кратки опис]
) <lst:recnici>

Речник _RELATIONSHIP_STRATEGY_DICTIONARY_ је укључен у _HOVER_DICTIONARY_ чиме се обезбеђује да се преласком миша преко
кључне речи испише опис новододатих кључних речи и стратегија. На слици @fig:hover приказан је кратки опис конструкта
_relationship_.

#figure(image("../slike/hover.png", width: auto),
caption: [
    Приказ кратког описа у _Visual Studio Code_-у.
]
)<fig:hover>

==== Предлог допуне текста

Поред кључних речи веза, предлог допуне текста проширен је и на стратегије веза. Посебно је значајно поменути
да се називи ентитета додају у листу могућих допуна па се након _from_ и _to_ могу понудити ентитети који су заиста
дефинисани у тренутној спецификацији.

Предлог допуна текста за пример стратегије генерисања веза је приказан на слици @fig:completion.

#figure(image("../slike/completion.png", width: auto),
caption: [
    Приказ предлога допуне у _Visual Studio Code_-у.
]
)<fig:completion>

==== Проналажење референци (енгл. _find references_)

За рад са везама, посебно је значајно проналажење референци. Оно омогућава проналажење свих места на којима се одређени
ентитет користи. Језички сервер најпре додаје саму дефиницију ентитета, а затим претражује употребу назива ентитета
у следећа три контекста (листинг @lst:konteksti):

#figure(
```python
patterns = [
    rf"ref\s+({re.escape(selected_word)})\b",
    rf"from\s+({re.escape(selected_word)})\b",
    rf"to\s+({re.escape(selected_word)})\b",
]
```,
    caption: [Контексти за проналажење референци]
) <lst:konteksti>

На овај начин се поред постојећих референци типа _ref_ обухватају и нове употребе ентитета у конструкцији _relationship_.
Пронађене позиције се враћају као _Location_ објекти које _Visual Studio Code_ може приказати кориснику.

На слици @fig:reference приказан је пример проналажења референци за ентитет _User_.

#figure(image("../slike/references.png", width: auto),
caption: [
    Приказ проналаска референци у _Visual Studio Code_-у.
]
)<fig:reference>

=== Интеграција са процесом генерисања

_Neo4j_ генератор не представља независан процес генерисања тестних вредности. Он користи исти поступак за генерисање
инстанци ентитета као и остали генератори. У имплементацији се зато поново користе функције из _sql_generator_ модула.
То укључује функције за прикупљање вредности, комбиновање, обраду експлицитно задатих случајева, одређивање редоследа
ентитета и препознавање референтних поља.

Након генерисања ентитета, _Neo4j_ генератор чува генерисане редове и идентификаторе, а затим на основу конструкција
_relationship_ формирају се везе између већ генерисаних чворова. Метода _build_relationships_ преузима полазне и циљне
редове, генерише својства веза и прослеђује све потребне параметре изабраној стратегији. 

На тај начин је постигнуто раздвајање одговорности. Заједнички механизам одређује вредности тестних података, док је _Neo4j_
генератор задужен за њихово организовање у графну структуру и формирање _Cypher_ излаза.

=== Формирање коначне датотеке

На крају процеса генерисања, функција _generate_neo4j_ преузима резултат генератора и уписује га у _Cypher_ датотеку.
Назив датотеке одређује се на основу назива шеме. Имплементација ове методе је дата у листингу @lst:upis_neo4j.

#figure(
```python
def generate_neo4j(model, output_dir, overwrite):
    timestamp = datetime.now().strftime("%Y-%m-%d %H:%M:%S")
    generator = Neo4JGenerator(
        model,
        timestamp=timestamp,
    )
    cypher = generator.render()
    schema_name = getattr(model, "name", "generated_data")
    file_path = os.path.join(
        output_dir,
        f"{schema_name}.cypher",
    )
    if os.path.exists(file_path) and not overwrite:
        raise FileExistsError(
            f"File {file_path} already exists. Use --overwrite to replace it."
        )
    with open(file_path, "w", encoding="utf-8") as f:
        f.write(cypher)
    return file_path
```,
    caption: [Функција за креирање _Cypher_ датотеке]
) <lst:upis_neo4j>

За шему ComprehensiveSystem, на пример, резултат се записује у датотеку:
```text
ComprehensiveSystem.cypher
```

Уколико датотека већ постоји, а није прослеђена опција _overwrite_ генератор пријављује грешку и не преписује постојећи
резултат. На овај начин, понашање _Neo4j_ генератора је усклађено са механизмом контроле постојећих излазних датотека.

=== Пример коначног резултата

Коначан _Cypher_ документ садржи најпре записе чворова, а затим записе веза између њих. Пример исечка садржаја дат је у листингу @lst:isecak.

#figure(
```cypher
// --- Node Definitions (records 100) ---
CREATE (:Course {id: "be7d8b4e-3152-41fb-b100-6ed23c570725", title: "De-engineered zero-defect parallelism", price: 0, capacity: 181});
CREATE (:Profile {id: "a6b48acc-f413-4c34-b97c-ca3c923140ce", bio: "Total myself it red discussion. Adult statement close out past.", birth_date: "2005-12-31", is_verified: true});
CREATE (:User {id: "f86c882c-f9af-4a56-8215-c2b76947c104", email: "cody82@example.com", username: "adamsdaniel", age: 98.0, status: "active"});

// --- Relationship Definitions (records 130) ---
MATCH (from:User {id: "f86c882c-f9af-4a56-8215-c2b76947c104"}), (to:Profile {id: "a6b48acc-f413-4c34-b97c-ca3c923140ce"})
CREATE (from)-[:USERHASPROFILE {linked_at: "2028-12-12T13:33:47"}]->(to);

MATCH (from:User {id: "f86c882c-f9af-4a56-8215-c2b76947c104"}), (to:Course {id: "be7d8b4e-3152-41fb-b100-6ed23c570725"})
CREATE (from)-[:STUDENTENROLLMENT {progress_percentage: 99.0, status: "dropped"}]->(to);
```,
    caption: [Исечак генерисаних података из _Cypher_ датотеке]
) <lst:isecak>

Оваква структура директно одговара моделу графне базе: _Course_, _Profile_ и _User_ представљају чворове, док _USERHASPROFILE_
представља усмерену везу између _User_-а и _Profile_-а. Својство _linked_at_ припада самој вези, а не неком од чворова.

Генерисана _Cypher_ скрипта из примера мастер рада је покренута у оквиру _Neo4j_ базе и приказани су следећи резултати:
- конкретна инстанца везе _StudentEnrollment_ из листинга @lst:isecak приказана је на слици @fig:primer
- својство _linked_at_ везе _StudentEnrollment_ приказано је на слици @fig:veza и
- веза _StudentEnrollment_ је приказан на слици @fig:graf.

#figure(image("../slike/primerNeo4j.png", width: auto),
caption: [
    Приказ инстанци чворова и веза из листинга @lst:isecak.
]
)<fig:primer>

#figure(image("../slike/veza.png", width: auto),
caption: [
    Детаљи везе _StudentEnrollment_ из генерисане _Cypher_ скрипте.
]
)<fig:veza>

#figure(image("../slike/studentenrollment.png", width: auto),
caption: [
    Све инстанце везе _StudentEnrollment_ из генерисане _Cypher_ скрипте.
]
)<fig:graf>

На овај начин _TestDataGen_ може да генерише не само скупове независних тестних записа, него и тестне графове са контролисаном
структуром повезаности. То је основна специфичност _Neo4j_ генератора у односу на _SQL_, _JSON_, _CSV_ и _YAML_ генераторе.

=== Интеграција са командном линијом

_Neo4j_ генератор је интегрисан у постојећи механизам командне линије и може се изабрати навођењем формата _neo4j_. На тај
начин се, у оквиру једног покретања система, исти _TestDataGen_ модел може користити за генерисање _Neo4j_ излаза заједно са
другим подржаним форматима.

Пример покретања:

```bash
testdatagen generate master.tdata --output ./output --format sql,json,report,csv,yaml,neo4j
```

На овај начин се једна спецификација обрађује кроз заједнички процес, а различити генератори преузимају исти концептуални
модел и формирају представу прилагођену конкретном излазном формату.

=== Значај имплементације _Neo4j_ генератора

Имплементацијом _Neo4j_ генератора _TestDataGen_ је проширен са генератора тестних података заснованих првенствено на
ентитетима на систем који може да генерише и структуру односа између генерисаних података. 

Основна разлика у односу на остале генераторе је у томе што се приликом генерисања не обрађују само вредности поља, већ
и везе између инстанци ентитета. Увођењем конструкције _relationship_ омогућено је декларативно описивање графне структуре,
док су стратегије  _one-to-one_, _one-to-many_, _many-to-one_ и _many-to-many_ омогућиле различите начине формирања
повезаности.

Истовремено, проширење граматике, валидације и _Visual Studio Code_ екстензије обезбеђује да нова функционалност буде 
интегрисана у цео процес рада са језиком _TestDataGen_, од писања спецификације, преко њене провере, до генерисања 
коначног _Cypher_ излаза.

На овај начин је очувана основна архитектура система: опис тестних података остаје одвојен од његове конкретне представе,
док се за _Neo4j_ формат, поред вредности ентитета, може описати и структура односа која је неопходна за генерисање
реалистичних тестних графова.
